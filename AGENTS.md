# 开发规范

## 核心原则：闭环交付

**写完代码不等于完成任务。** 每个任务的完成标准是：能自主测试、能判断问题根源、能修复（代码或用例），反复迭代直到验证通过，最终交付用户一个可靠版本。

---

## 开发流程

```
实现 → 自测 → 发现问题 → 判断根源 → 修复 → 再自测 → … → 交付
```

每一轮循环都必须有日志/输出作为判断依据，不靠猜测。

### 1. 实现

- 只做任务要求的，不引入额外抽象或假设的未来需求
- 优先改已有文件，不随意新建
- 参考 `CLAUDE.md` 中的具体规范

### 2. 自测

**必须在报告完成前自己跑通测试。** 测试分层：

| 层次 | 方式 | 适用场景 |
|------|------|----------|
| 单元 | 直接运行脚本/函数，检查返回值 | 纯逻辑，无 UI |
| 集成 | 启动服务 + 脚本模拟调用，检查日志 | 有 IPC/socket/daemon |
| E2E | 脚本驱动 UI（JS inject、osascript、accessibility）+ 日志验证 | 有浏览器/GUI |

**什么不算测试：**
- 只看代码逻辑说"应该可以"
- 只看 compilation/lint 通过
- 靠用户反馈来发现基本 bug

### 3. 判断问题根源

出现问题时，先从日志/输出读懂实际行为，再对比预期行为，**然后才改代码**。  
常见误区：
- log 显示成功但功能不对 → 可能 log 在错误路径上，或测试验证点有误
- 用例通过但用户说不行 → 用例覆盖的场景不完整，补场景而不是强行改代码

### 4. 修复

修复可能是：
- **改代码**：逻辑错误、API 使用不当、线程问题
- **改用例**：验证点过时（如 log 字符串变了）、场景覆盖不足
- **两者都改**：逻辑错同时测试也遗漏了边界

### 5. 交付标准

- 自测全部用例通过
- 日志无 ERROR/WARNING（或有明确说明为什么可以忽略）
- 用例本身存入 `cases/` 目录，供后续回归

---

## E2E 测试规范

### 原则

- 测试脚本必须能**无人值守**运行，不依赖用户手动操作
- UI 交互优先用 JS inject（`native_eval`）或 osascript，而不是要求用户点击
- 验证点基于**可观测事实**（日志字符串、返回值、按钮状态），不基于"应该有声音"这类主观感受
- 如果某个验证点主观上无法自动化（如"听起来有声音"），在用例里显式标注为"需人工确认"，并说明前置条件

### 测试驱动调试循环示例

```python
# 1. 发现问题：log 显示 completion 触发，但功能不对
#    → 说明 completion 和实际状态变更可能解耦（如 thread 问题）
# 2. 假设：API 需要 main thread，background thread 调用静默失败
# 3. 验证假设：加 timeout log，观察 completion 是否在预期时间内触发
# 4. 换方案：绕开 API，改用 GT 自己的 Stop 按钮（保持 GT 状态机一致）
# 5. 验证新方案：重跑用例，按钮状态变化（Listen → Stop listening）可观测
```

### Socket/Daemon 类服务的测试模式

给 daemon 加一个 `{"action": "eval", "code": "..."}` 指令，允许从外部注入 JS 并同步拿到结果。这样测试脚本可以：
- 查询页面状态（按钮标签、DOM 结构）
- 模拟用户交互（click）
- 验证副作用（状态变化）

---

## Cases 目录规范

每个项目/模块的测试记录存在该模块目录下：

```
translate-panel/
  tests/
    cases/
      001-audio-flow/
        README.md      ← 场景描述、步骤、预期结果、已知陷阱
        test_*.py      ← 可运行的自动化脚本
      002-text-inject/
        ...
```

Case 内容要包含：
1. **场景**：用自然语言描述用户行为链
2. **自动化步骤**：可复现的脚本命令
3. **验证点**：哪些 log/状态是成功标准
4. **已知陷阱**：调试过程中踩过的坑，避免后人重踩

---

## 参考

- 项目级规范：`CLAUDE.md`
- Skills 规范：`CLAUDE.md` → Skills 部分
